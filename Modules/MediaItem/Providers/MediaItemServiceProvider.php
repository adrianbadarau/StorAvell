<?php

namespace Modules\MediaItem\Providers;

use Illuminate\Support\ServiceProvider;
use Modules\MediaItem\Entities\MediaItem;
use Route;

class MediaItemServiceProvider extends ServiceProvider
{
    /**
     * Indicates if loading of the provider is deferred.
     *
     * @var bool
     */
    protected $defer = false;

    /**
     * Boot the application events.
     *
     * @return void
     */
    public function boot()
    {
        $this->registerTranslations();
        $this->registerConfig();
        $this->registerViews();
        Route::model('mediaitem',MediaItem::class);
    }

    /**
     * Register the service provider.
     *
     * @return void
     */
    public function register()
    {
        //
    }

    /**
     * Register config.
     *
     * @return void
     */
    protected function registerConfig()
    {
        $this->publishes([
            __DIR__.'/../Config/config.php' => config_path('mediaitem.php'),
        ]);
        $this->mergeConfigFrom(
            __DIR__.'/../Config/config.php', 'mediaitem'
        );
    }

    /**
     * Register views.
     *
     * @return void
     */
    public function registerViews()
    {
        $viewPath = base_path('resources/views/modules/mediaitem');

        $sourcePath = __DIR__.'/../Resources/views';

        $this->publishes([
            $sourcePath => $viewPath
        ]);

        $this->loadViewsFrom(array_merge(array_map(function ($path) {
            return $path . '/modules/mediaitem';
        }, \Config::get('view.paths')), [$sourcePath]), 'mediaitem');
    }

    /**
     * Register translations.
     *
     * @return void
     */
    public function registerTranslations()
    {
        $langPath = base_path('resources/lang/modules/mediaitem');

        if (is_dir($langPath)) {
            $this->loadTranslationsFrom($langPath, 'mediaitem');
        } else {
            $this->loadTranslationsFrom(__DIR__ .'/../Resources/lang', 'mediaitem');
        }
    }

    /**
     * Get the services provided by the provider.
     *
     * @return array
     */
    public function provides()
    {
        return [];
    }
}
